{
  "name": "ai-command-center",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.scripts.json",
    "mock": "node scripts/gen_mock.ts",
    "types": "wrangler types"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "wrangler": "^4.103.0",
    "@types/node": "^24.0.0"
  }
}
